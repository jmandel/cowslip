# Public and safe to expose in a static GitHub Pages build.
BUN_PUBLIC_INSTANT_APP_ID=

# Optional: only for local/admin scripts if we add a server-side command layer later.
# Never expose this in GitHub Pages client bundles.
INSTANT_APP_ADMIN_TOKEN=
